specVersion: "v1beta"
serviceId: "all-sports-db-id"
location: "us-east1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "all-sports-db"
      cloudSql:
        instanceId: "all-sports-db"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
